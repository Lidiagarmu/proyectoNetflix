package org.example.view;


import javax.swing.*;
import java.awt.*;

public class AdminPanel extends JPanel {
    public AdminPanel() {
        setLayout(new BorderLayout());
        add(new JLabel("Admin Panel"), BorderLayout.CENTER);
        // Añadir componentes específicos para Admin
    }
}